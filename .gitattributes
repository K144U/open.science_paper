# Normalize text to LF in the repository.
* text=auto eol=lf

# Cluster scripts must stay LF or they break under bash/PBS on Linux.
*.sh   text eol=lf
*.pbs  text eol=lf
*.py   text eol=lf

# Binary assets: never touch.
*.pdf  binary
*.png  binary
*.pt   binary
